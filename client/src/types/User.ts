export type User = {
    id: string;
    username: string;
    email: string;
    avatar: string;
};

export type RegisterUser = {
    id: string;
    username: string;
    email: string;
    password: string;
};
